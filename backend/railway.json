{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node dist/main",
    "healthcheckPath": "/api/v1/rag/stats",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE"
  }
}
