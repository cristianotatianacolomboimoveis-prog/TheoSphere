{
  "name": "theosphere-edge",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker — edge cache in front of the TheoSphere API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250505.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
