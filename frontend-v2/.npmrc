# TheoSphere runs Hugging Face Transformers in a browser worker.
# Skip ONNX Runtime Node's automatic CUDA binary download during CI/Vercel installs.
# Browser inference uses onnxruntime-web; no Node CUDA runtime is required by the frontend.
onnxruntime-node-install=skip
