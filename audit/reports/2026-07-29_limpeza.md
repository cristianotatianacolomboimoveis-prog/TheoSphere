# Limpeza de plataformas e repositório — 2026-07-29

Commit `1a6ff12`. Produção revalidada depois de tudo: backend e frontend respondendo 200.

---

## Vercel — de 3 projetos para 1

| Projeto            | Situação                                                                                                                       | Ação        |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ----------- |
| `frontend-v2`      | a plataforma (157 saídas de build)                                                                                             | **mantido** |
| `cristianocolombo` | publicava a **raiz do monorepo** — uma única função `index` de 2.89MB, não o app. Estava ligado ao Git e disparava a cada push | **apagado** |
| `backend`          | último deploy há 17 dias com status `Error`. O backend real roda no Render                                                     | **apagado** |

Removi também o `.vercel/` da raiz do repositório, que era o link para o projeto apagado. É o que fazia a raiz ser deployável por engano.

> ⚠️ As URLs `cristianocolombo.vercel.app` e `backend-*.vercel.app` deixaram de existir. Se algum testador recebeu um desses links, mande o correto: **https://frontend-v2-lake.vercel.app**

---

## GitHub — de 4 branches para 1

| Branch                          | Estado                                                                       | SHA (para recuperar)                       |
| ------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------ |
| `main`                          | ativa                                                                        | —                                          |
| `feature/ci-cd-improvements`    | 100% incorporada                                                             | `51334c68af078e7545f06e5a1288acb240168cc8` |
| `feature/setup-dev-tools`       | 100% incorporada                                                             | `183c5843bd58573994b4d454954611cd06260a84` |
| `cloudflare/workers-autoconfig` | 1 commit nunca incorporado, de 04/06: "Add Cloudflare Workers configuration" | `fbbc4a26376e7b93544b4bfac0b922c114c0e629` |

Enquanto o GitHub não coletar os objetos, dá para trazer qualquer uma de volta:

```bash
git fetch origin fbbc4a26376e7b93544b4bfac0b922c114c0e629
git branch cloudflare/workers-autoconfig FETCH_HEAD
```

---

## Repositório — 45 artefatos gerados fora do versionamento

**Removidos** (24 em `prisma/`, 21 no resto do backend):

- `prisma/seed-*.{js,d.ts,js.map}` — os scripts do `package.json` chamam `ts-node` direto no `.ts`; os compilados nunca eram usados
- `.d.ts` e `.js.map` de `clear-cache`, `test-gemini`, `test-gemini-config`, `jest.config`
- `test-ai-service.{js,d.ts,js.map}` — tem fonte `.ts`
- `jest.config.ts.bak`
- `scratch/*.{d.ts,js.map}`

**Preservados de propósito** — e isso importa: `jest.config.js`, `clear-cache.js`, `test-gemini.js` e `test-gemini-config.js` **não têm fonte `.ts`**. Eles não são compilados: são o código em si. Apagar o `jest.config.js` teria quebrado a suíte de testes inteira. Os `.js` de `scratch/` também ficaram, conforme combinado.

**PDF de arquitetura:** o git tinha **duas entradas** para o mesmo arquivo — os nomes pareciam idênticos mas um estava em NFD e outro em NFC (formas diferentes de codificar o `ç` e o `ã`). Renomeei para `TheoSphere-Arquitetura-IDX.pdf`, sem acento, e liguei `core.precomposeunicode` para não voltar a acontecer.

**`.gitignore` do backend** passa a barrar `*.d.ts` (exceto `src/`), `*.js.map` e `*.ts.bak`.

---

## Disco local — 103 MB

- `frontend-v2/.next_stale` (66 MB) — build antigo do Next
- `.claude/worktrees` (37 MB) — worktree órfã de um agente, já marcada como `prunable`
- `tsconfig.build.tsbuildinfo` e `test/coverage`

**Mantido:** `valhalla_data/` (116 MB). É o mapa OSM de Israel/Palestina que o `docker-compose.valhalla.yml` monta para o roteamento do Atlas 4D — dado do projeto, e o `scripts/setup_valhalla.sh` levaria tempo baixando de novo.

---

## Sobrou para você decidir

**`.venv/` (148 MB) e dois scripts Python órfãos.** `scripts/daily_verifier_agent.py` importa `google.antigravity` — é uma implementação antiga do verificador diário, hoje substituída pela task agendada do Claude. Nada no repositório referencia ele nem `audit/reports/test_rag_pipeline.py`. Ficaram porque estão fora do escopo de limpeza que combinamos (compilados + duplicado). Se concordar que são mortos, some `rm -rf .venv` e mais 148 MB.

**`GEMINI_API_KEY` no Render** — segue sendo o bloqueador de verdade, e é o único item que eu não consigo executar: é um segredo, e só existe no dashboard. Detalhes e comando de validação no relatório de deploy.

---

## Estado final

```
GitHub    main @ 1a6ff12, branch única, working tree limpa
Vercel    frontend-v2 (único projeto)
Render    backend no ar — health, passage-guide, versions, search: HTTP 200
Testes    backend 96 · frontend 49 · static-checks limpo · tsc limpo
```
