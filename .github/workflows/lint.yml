# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — This workflow is superseded by ci.yml which already runs
# lint + prettier checks for both backend and frontend.
#
# This file is kept as a stub to avoid breaking any branch protection rules
# that reference the "Lint & Format Check" workflow name.
# ─────────────────────────────────────────────────────────────────────────────
name: Lint & Format Check

on:
  workflow_dispatch: # Manual-only — no longer triggers on push/PR

jobs:
  notice:
    name: Redirected to CI
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint/format now run in the CI workflow (ci.yml). This workflow is deprecated."
