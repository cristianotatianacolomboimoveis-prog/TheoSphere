# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — This workflow is superseded by ci.yml which already runs
# lint + typecheck + tests + migration-drift in a single pipeline.
#
# This file is kept empty to avoid breaking any branch protection rules
# that reference the "Test Suite" workflow name. Remove it once branch
# protections are updated.
# ─────────────────────────────────────────────────────────────────────────────
name: Test Suite

on:
  workflow_dispatch: # Manual-only — no longer triggers on push/PR

jobs:
  notice:
    name: Redirected to CI
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests now run in the CI workflow (ci.yml). This workflow is deprecated."
